import java.util.Scanner;
class Main {
    static public void main (String args[])
    {
        try{
            Scanner scan = new Scanner(System.in);
            System.out.println("Could you Enter the charachter gonna replace White places.");
            String swhite = scan.nextLine();
            System.out.println("Could you Enter the charachter gonna replace Balck places.");
            String sBlack = scan.nextLine();
            if (swhite.length > 1 || sBlack.length > 1)
                return ;
            FileReader fileReader = new FileReader()
        }
    }
}